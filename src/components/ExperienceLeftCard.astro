---
interface Props {
    month: string;
}

const { month } = Astro.props;
---

<article class="text-white text-right mr-5 mb-5">
    <div class="mr-2 font-bold">{month}</div>
    <div class="mr-7">
        <div class="flex flex-row gap-2 items-center justify-end">
            <slot/>
        </div>
    </div>
</article>

<style>
    article:hover {
        filter: drop-shadow(0 0 5px #ffff02); 
        transition: color 0.3s;
    }
</style>